import SearchBar from "./SearchBar";

const Widgets = () => {
    return (
        <aside className="hidden lg:block xl:w-[600px]">
            {/* Search */}
            <SearchBar />
        </aside>
    );
};

export default Widgets;
